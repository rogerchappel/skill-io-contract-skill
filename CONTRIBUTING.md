# Contributing

Keep changes local-first and fixture-backed. New checks should include at least one passing fixture and one failing fixture when practical.

Before opening a PR, run:

```bash
bash scripts/validate.sh
```


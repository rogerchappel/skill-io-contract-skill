{
  "name": "skill-io-contract-skill",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first IO contract checker for reusable agent skills.",
  "scripts": {
    "test": "python3 -m pytest",
    "check": "python3 -m compileall src tests",
    "smoke": "python3 -m skill_io_contract check --skill SKILL.md --fixtures fixtures/cases.json --report /tmp/skill-io-contract-report.md"
  }
}
